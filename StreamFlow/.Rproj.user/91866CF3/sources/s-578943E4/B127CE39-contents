return_level <- function(fit,p){
  fit <- fit_list[[7]]
  p=1/2
  yp <- -log(1-p)
  mu <- fit$est_mu[,2:4]
  sig <- exp(fit$est_sig[,2:4])
  sh <- fit$est_sh[,2:4]
  r_l <- mu-sig/sh*(1-yp^(-sh))
  r_l$Year <-  1980:2019
  names(r_l) <- c("est","U","L","Year")
  return(r_l)
}